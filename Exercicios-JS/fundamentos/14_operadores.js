//atribuições
{
    const a = 7;
    let b = 3;

    // b = b + a
    b += a;
    console.log(b);

    // b = b - 4
    b -= 4;
    console.log(b);

    // b = b * 2
    b *= 2;
    console.log(b);

    // b = b / 2
    b /= 2;
    console.log(b);

    // b = b % 2
    b %= 2;
    console.log(b);
}

