const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;

const subtracao = d - b;

const multiplicacao = a * b;

const divisao = d / a;

//se o resto da divisão for zero é par se for 1 é ímpar
const modulo = a % 2;

console.log(soma,subtracao,multiplicacao,divisao,modulo);