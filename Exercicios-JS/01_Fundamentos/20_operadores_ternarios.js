//primeira parte a expressão que retorna verdadeiro ou falso
// expressão ? 'retorno verdadeiro' : 'retorno falso'

const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';

console.log(resultado(7.1));
console.log(resultado(6.1));
